<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
      <%= render(:partial =>'user',:locals => {:user => user}) %>
  <% end %>
</ul>
<%= will_paginate %>

